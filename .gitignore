# local build scratch (cloned sources, extracted trees)
.work/

# abuild per-package scratch
testing/*/src/
testing/*/tmp/

# built artifacts
*.apk
*.tar.gz

# draft Alpine-MR notes — kept locally, never pushed
SUBMISSION.md
ISSUE.md
