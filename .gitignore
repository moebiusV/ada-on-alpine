# local build scratch (cloned sources, extracted trees)
.work/

# abuild per-package scratch
testing/*/src/
testing/*/tmp/

# built artifacts
*.apk
*.tar.gz

# draft Alpine-MR notes — kept locally, never pushed
SUBMISSION.md
ISSUE.md

# Ada library build scratch (gprbuild obj/lib/exec artifacts)
sources/*/obj/
sources/*/lib/
sources/*/obj-check/
sources/*/tests/*_check
