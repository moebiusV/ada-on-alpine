# names are resolved later by the ntp engine: one entry each, no address
servers pool.example.org
server ntp.example.org weight 3
constraints from "https://time.example.com/some/path"
constraints from "time.example.net"
constraint from "https://www.example.org" 192.0.2.1 2001:db8::2
