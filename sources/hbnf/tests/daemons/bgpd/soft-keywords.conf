# `no` and `yes` are STRINGs to bgpd's parse.y (yesno compares them), not
# keywords, so `send no` parses.
AS 1

neighbor 192.0.2.1 {
	remote-as	64496
	announce add-path recv no
	announce add-path send no
	announce as-4byte yes
}
