# `include` reads the file's statements in its place (the path is relative
# to where the parser runs, as in pfctl); its macros are the config's.
include "include.inc"
pass in on $inc_if from $inc_net
