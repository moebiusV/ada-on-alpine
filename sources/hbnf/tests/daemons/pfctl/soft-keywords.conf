# Words parse.y reads as a STRING and compares in an action are not
# keywords (the `keywords` table has only parse.y's lookup() words), so
# they can be values: `none` here, and optimizer's `basic`.
set loginterface none
set ruleset-optimization basic
set optimization normal
pass in on lo0 tagged profile
