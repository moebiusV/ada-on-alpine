# `https` is a STRING to relayd's parse.y (http_type compares it), not a
# keyword, so it can name a protocol.
http protocol https {
	match request header set "Connection" value "close"
}
relay wwwtls {
	listen on 127.0.0.1 port 8443 tls
	protocol https
}
