# `dot` is a jet (its own token kind, not a keyword): an alternation whose
# other branches are keyword-led must still try it.  The keyword dispatch
# used to fail these fast.
preference DoT
preference { DoT recursor }
preference tls
